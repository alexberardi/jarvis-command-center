# Eval run artifacts (regenerated by the eval scripts). The scenario corpus
# (memory_eval_scenarios.json) is the INPUT and is tracked.
extraction_eval_*.json
recall_eval_*.json
ex_t0*.json
ambient_eval_*.json
