"""Offline evaluation harnesses for jarvis-command-center.

Not part of the shipped service — these are manual/CI measurement tools that
exercise real service code against a real (or mocked) model. Named ``evals``
(not ``eval``) to avoid shadowing the ``eval`` builtin.
"""
